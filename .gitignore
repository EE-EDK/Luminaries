node_modules/
dist/
# Vite outDir (vite.config.js). Pages builds it from source in the workflow; never commit it.
docs/
.DS_Store
*.log

# Python
.venv/
venv/
__pycache__/
*.pyc

# Environment
.env

# Graphify transient cache artifacts
graphify-out/cache/

# Delivery archives (snapshots handed to the owner, never source)
*.zip

# Files delivered into the chat land here when a folder is connected
Claude outputs/
